# HashWeb Environment Variables

# Redis connection URL (will be auto-provided by Railway Redis service)
REDIS=redis://redis:6379

# Maximum file size limit
SIZE_LIMIT=10MiB

# Logging verbosity (debug, info, warn, error)
VERBOSITY=info

# Frontend path (already set in Dockerfile)
# FRONTEND_PATH=./frontend
